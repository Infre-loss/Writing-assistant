# 「写作助手」项目备份使用说明

> 这个项目（D:\新建文件夹）已纳入 **Git 版本管理**，并连接你的 GitHub 私有仓库。
> 目的：随时**保存进度**，改坏了可以**回到上一次备份**，电脑坏了数据也还在 GitHub 上。

---

## 这个仓库里存了什么？

- 需求文档、使用说明（.md 文件）
- 写作助手的全部源代码（writing-assistant 文件夹，不含安装包/程序库/缓存）

## 两个常用操作

### 📤 保存进度（每次完成一个阶段后做一次）
**双击 `一键备份.bat`** 即可：
1. 把当前所有改动保存为一个新版本
2. 自动上传到 GitHub（云端留底）

### ↩️ 出错回退（改坏了想回到上次备份）
**双击 `恢复到上次备份.bat`**，确认后：
- 项目会回到**上次备份**时的状态
- 上次备份之后的改动会丢失（所以重要节点先备份再继续写）

## 用 VSCode 看版本历史（可选，更直观）

1. 打开 VSCode → 文件 → 打开文件夹 → 选择 `D:\新建文件夹`
2. 左侧点「源代码管理」图标：能看到当前改了哪些文件、可手动提交
3. 点「时间线」面板或装 Git Graph 扩展，可查看所有备份版本
4. 在 GitHub 网页打开你的仓库 → commits，也能看到全部版本

## 常见问题

**Q：GitHub 上的内容别人能看到吗？**
不会。只要创建仓库时选了 **Private（私有）**，只有你能看。

**Q：我的小说/写作数据在备份范围里吗？**
不在。写作数据存在 C 盘用户目录（AppData\Roaming\writing-assistant\data），本仓库只备份开发项目。
如果你以后也想给写作数据做备份，可以告诉我再加。

**Q：一键备份后，我改的东西怎么没了？**
请区分两个按钮：改完想**保存** → 一键备份；改坏了想**回退** → 恢复到上次备份。
回退前脚本会再次提醒你。

**Q：推送 GitHub 失败？**
① 检查是否已连接远程仓库（见主对话中的连接步骤）；② 首次推送需要登录 GitHub 授权；③ 电脑需能上网。
