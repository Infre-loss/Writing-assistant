# 「写作助手」发布更新说明（开发者用）

> 软件内置「检查更新」：启动自动检查一次，顶栏也有「⤓ 检查更新」按钮。
> 原理：软件访问一个公开网址上的 `version.json`，若其中的版本号高于当前版本，就提示下载。

---

## 一、一次性配置（注册好 Gitee 后做一次）

1. 在 [Gitee](https://gitee.com) 注册并新建一个**公开**仓库（如 `writing-assistant`）
2. 把软件里的更新地址填进去：打开 `writing-assistant\main\config.js`，
   把 `UPDATE_MANIFEST_URL` 改为你的清单地址：
   ```js
   UPDATE_MANIFEST_URL: 'https://gitee.com/你的用户名/writing-assistant/raw/master/version.json'
   ```
3. 重新打包发布（这样用户拿到的版本才带正确的检查地址）

## 二、version.json 格式（放在 Gitee 仓库根目录）

```json
{
  "version": "0.2.0",
  "note": "本次更新：\n1. 新增写作台\n2. 修复 AI 润色抓取不完整",
  "downloadUrl": "https://gitee.com/你的用户名/writing-assistant/raw/master/写作助手%20Setup%200.2.0.exe"
}
```

- `version`：新版本号（必须大于软件当前版本号才会提示）
- `note`：更新说明，会自动换行显示
- `downloadUrl`：下载地址（Gitee 文件直链；文件名里的空格要写成 `%20`）

## 三、每次发版的流程

1. 改 `package.json` 里的 `version`（如 0.2.0）
2. 重新打包：`npm run pack`（绿色版）+ `npm run pack:installer`（安装版）
3. 到 Gitee 仓库网页 → 上传 `写作助手 Setup X.Y.Z.exe` 和 `version.json`
4. 改 `version.json` 里的版本号/说明/下载地址，上传覆盖旧的
5. 完事 —— 用户打开软件（或点「检查更新」）就会看到新版本提示

## 四、测试小技巧

开发时可以临时用环境变量覆盖更新地址，例如本地起一个测试服务器：

```
set DS_UPDATE_URL=http://127.0.0.1:8899/version.json
npm start
```

`version.json` 里写一个很大的版本号（如 9.9.9）即可验证"发现新版本"弹窗。
