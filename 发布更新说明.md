# 「写作助手」发布更新说明（开发者用）

> 软件内置「检查更新」：启动自动检查一次，顶栏也有「⤓ 检查更新」按钮。
> 原理：软件访问 GitHub 公开仓库里的 `version.json`，若版本号高于当前版本，就提示下载。

---

## 一、发布位置（已配置好）

- **更新清单**：`https://raw.githubusercontent.com/Infre-loss/Writing-assistant/master/version.json`
- **安装包存放**：本仓库 `downloads/0.2.0/`（GitHub 公开仓库，用户可直接下载）
- 软件里的地址在 `writing-assistant\main\config.js` 的 `UPDATE_MANIFEST_URL`（已指向 GitHub）

## 二、version.json 格式（本仓库根目录）

```json
{
  "version": "0.2.0",
  "note": "本次更新：\n1. 新增写作台…",
  "downloadUrl": "https://github.com/Infre-loss/Writing-assistant/raw/master/downloads/0.2.0/writing-assistant-setup-0.2.0.exe"
}
```

- `version`：新版本号（必须大于软件当前版本号才会提示）
- `note`：更新说明，会自动换行显示
- `downloadUrl`：安装包直链（GitHub 文件直链）

## 三、每次发版的流程

1. 改 `writing-assistant/package.json` 里的 `version`（如 0.3.0）
2. 重新打包：`npm run pack`（绿色版）+ `npm run pack:installer`（安装版）
3. 把两个新包复制到仓库根目录的 `downloads/0.3.0/`（新版本建新文件夹）
4. 更新根目录 `version.json`：版本号、说明、下载地址（指向 `downloads/0.3.0/…`）
5. 提交并推送 GitHub：
   ```
   D:\Git\cmd\git.exe add -A
   D:\Git\cmd\git.exe commit -m "0.3.0 发布"
   D:\Git\cmd\git.exe push origin master
   ```
6. 完事 —— 用户打开软件（或点「检查更新」）就会看到新版本提示

> 注意：安装包会进入 git 历史（单版本约 190MB），GitHub 已提示可改用 Git LFS。若仓库过大时可考虑改用 GitHub Releases 附件（网页版创建发行版挂附件，仓库体积不增加）。

## 四、测试小技巧

开发时可以临时用环境变量覆盖更新地址，例如本地起一个测试服务器：

```
set DS_UPDATE_URL=http://127.0.0.1:8899/version.json
npm start
```

`version.json` 里写一个很大的版本号（如 9.9.9）即可验证"发现新版本"弹窗。
