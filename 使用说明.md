# 「写作助手」使用说明（面向作家用户）

这是一份写给不懂代码的使用者的操作指南。软件功能不多，核心就三件事：
**整理大纲 → 标记进度 → 用 AI 润色文字**。你的所有大纲和作品数据都只保存在你自己的电脑里。

---

## 1. 安装与启动

- **方式一（安装包）**：拿到 `写作助手 Setup.exe` 后双击安装，安装完桌面会出现「写作助手」图标，双击打开即可。
- **方式二（绿色版）**：拿到 `写作助手.exe`（portable 单文件版）后，放到任意文件夹，双击即可运行，无需安装。
- 第一次打开会看到欢迎页，点击「新建第一个作品」即可开始。

## 2. 界面一览

顶部从左到右依次是：

| 区域 | 作用 |
|------|------|
| ✍️ 写作助手 | 软件名 |
| 作品下拉框 ＋ ✎ 🗑 | 切换作品 / 新建作品 / 重命名 / 删除 |
| 进度条 + 文字 | 当前作品总进度（完成章数、总字数） |
| 大纲 / 时间轴 / AI 助手 | 三个页签，随时切换 |
| ⬇ MD / ⬇ TXT | 导出当前作品为 Markdown 或 TXT 文件 |

## 3. 搭建大纲（大纲页签）

1. 点击左侧「＋ 添加章节」，创建"第一章"等节点
2. 每行两行布局：第一行是章节名，第二行是**状态文字**（未开始/写作中/已完成，点击可切换）+ 字数进度
3. 在右侧详情面板可以：
   - 修改标题（也可以直接在左侧双击标题改名）
   - 标记**写作状态**：未开始 / 写作中 / 已完成
   - 填写**目标字数**和**已写字数**（写作台会自动更新已写字数）
   - 填写**备注**：人物设定、伏笔、场景要点等（类似写作卡片）
   - 填写**里程碑**：如"高潮""真相揭露"，会以 🏁 显示
   - 设置**时间轴顺序**：数字越小越靠前，0 表示按大纲顺序
4. 层级随意：鼠标悬停在节点上会出现 ＋（加子节点）、⇣（加同级）、🗑（删除）
5. **拖拽调整**：按住节点上下拖动，放到目标节点的上方/中间/下方，即可调整顺序或层级
6. 所有改动**自动保存**，无需手动保存

> 小技巧：想搭"卷 → 章 → 节"三层结构，就在"卷"上点 ＋ 加"章"，在"章"上点 ＋ 加"节"。

## 4. 在写作台创作正文（写作台页签）

1. 顶部页签点「**写作台**」（或在大纲选中章节后，详情面板点「✍ 去写作台创作」）
2. 顶部下拉框可切换章节；中间大区域直接写正文
3. 右上角实时显示**字数**，正文改动自动保存（会同步到大纲的「已写字数」）
4. 底部三个按钮：
   - **⬇ 导出本章为 Word**：当前章节导出为 .docx 文档
   - **⬇ 导出全书为 Word**：全书所有章节合成一份 Word，章节名自动作为标题
   - **⬆ 导入 Word 继续写**：选一个 .docx 文件，正文进入编辑区可直接接着写，字数自动回填大纲
5. 说明：只支持 **.docx** 格式；老 .doc 文件请先在 Word 里"另存为 .docx"再导入

## 5. 查看时间轴（时间轴页签）

- 时间轴按**故事发生顺序**排列你的章节，和目录顺序可以不同
- **层级缩进**：卷在最左，章缩进一档，节再缩进一档
- **点击卡片任意位置，直接进入写作台创作该章节**
- 有里程碑的章节会显示 🏁 标记
- 卡片上：第一行章节名（右侧「✍ 去写作」），第二行写作状态与字数
- 故事顺序在**大纲**选中章节后的详情面板里设置「时间轴顺序」

## 6. 用 AI 润色文字（AI 助手页签）

> ⚠️ 隐私说明：**大纲和作品数据永远不会被上传**。只有你**手动粘贴**到 AI 输入框里的那段文字，才会被发送到 DeepSeek 官方网页版。

### 首次使用（一次性设置，约 1 分钟）

1. 打开「AI 助手」页签，点击「打开浏览器」按钮
2. 电脑上会弹出 Edge 浏览器窗口并打开 DeepSeek 网页版
3. 在窗口里**用你的手机号登录 DeepSeek**（如果没登录过，先注册）
4. 登录成功后回到软件，点「检查登录」，显示"已登录 ✓"即可
5. 之后每次使用都会自动复用这次登录，**不需要每次重新登录**

### 日常润色

1. 把要润色的段落**粘贴**到输入框（支持 1000 字以上的长文，生成时间会稍长）
2. 点选润色要求（更流畅 / 更简洁 / 更文艺 / 更口语化 / 扩写 / 压缩），也可以填自定义要求
3. 点击「发送润色」，等 AI 生成（短文几秒到半分钟，长文可能几分钟）
4. 结果出来后点「复制结果」，粘贴回你的文稿即可

### 如果出错怎么办

| 现象 | 解决办法 |
|------|----------|
| "未检测到 Edge 或 Chrome" | 需要先安装微软 Edge 浏览器（Windows 一般自带） |
| "尚未登录" | 点「打开/恢复浏览器」，在窗口中登录后点「检查登录」 |
| 提示"使用限制" | DeepSeek 网页版每日有免费提问上限，过段时间再试 |
| 回复空白/超时 | 检查浏览器窗口是否弹出、是否已登录，再试一次 |

## 6. 数据与备份

- 所有数据存在你电脑上（系统用户目录的 `writing-assistant\data` 文件夹），**卸载软件不会删数据**
- 建议定期点顶部的 **⬇ MD / ⬇ TXT** 导出，作为备份
- 导出的文件是纯文本，可以用记事本、Word、任何写作软件打开

## 7. 常见问题（FAQ）

**Q：需要联网吗？**
大纲和进度功能不需要联网；只有 AI 润色需要联网。

**Q：我的大纲会被 AI 看到吗？**
不会。大纲与 AI 模块完全隔离，AI 只能看到你粘贴到输入框的文字。

**Q：可以同时写几本书？**
可以。用顶部的作品下拉框切换，想建多少个作品都行。

**Q：字数会自动统计吗？**
字数由你自己填写"已写字数"来更新（比如写完一章填一次）。如果你希望以后自动统计正文，可以提需求，后续版本可加"正文编辑"功能。

---

*软件仍在完善中，遇到问题或想加功能，随时告诉开发者。*
