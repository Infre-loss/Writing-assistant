# 辅助写作软件 —— 需求文档（v1.0 已确认）

> 状态：需求已确认 ✓　下一步：开发
> 文档更新时间：2026-01

## 〇、确认记录（2026-01，用户逐项确认）

| # | 问题 | 确认结果 |
|---|------|----------|
| 1 | AI 接入方式 | **自动操作 DeepSeek 网页版**（用户已知悉风险） |
| 2 | 大纲生成方式 | **纯手动搭建**（大纲 100% 留在本地） |
| 3 | 大纲视图 | **树状图 + 时间轴，两种都要，可切换** |
| 4 | 进度标记粒度 | **章节级**（状态 + 字数） |
| 5 | 多作品管理 | **需要**（一个软件管理多个作品） |
| 6 | 导出格式 | **Markdown + TXT** |
| 7 | AI 润色文字来源 | **手动粘贴到聊天框** |
| 8 | DeepSeek 账号 | **已有账号** |
| 9 | 浏览器 | **不确定 → 软件启动时自动检测本机 Chrome / Edge，并允许手动选择** |
| 10 | 需求确认 | **全部确认，开始开发**（2026-01） |

## 一、软件定位

运行在 Windows 电脑上的**桌面辅助写作软件**，面向作家群体，解决三个核心问题：

1. 把零散的写作灵感/想法，整理成清晰有条理的大纲（纯本地，不联网）
2. 直观看到每个章节的写作进度和整个作品的总进度
3. 在**与大纲完全隔离**的独立模块里，通过自动操作 DeepSeek 网页版，辅助润色文字

## 二、功能模块

### 模块 1：大纲管理（核心功能，纯本地）

- **多作品项目**：一个作品一个项目，可新建、切换、重命名、删除
- **大纲结构**：多层级目录「卷 → 章 → 节」（层级可多可少，支持折叠展开）
  - 新建、删除、重命名、复制章节
  - 拖拽调整顺序（可跨层级移动）
  - 任意章节可附加**备注卡片**（人物设定、伏笔、场景要点、灵感记录等）
- **创建方式：纯手动搭建**，不调用任何 AI，数据不出本机
- **双视图展示（同一份数据，随时切换）**：
  - **树状图**：像目录一样一层层展开，适合看整体结构
  - **时间轴**：按故事发生的时间顺序横向排列章节/事件，适合看叙事脉络，可标记里程碑（如"写到高潮"）

### 模块 2：写作进度追踪（核心功能）

- **章节级状态**：每章标记「未开始 / 写作中 / 已完成」，一键切换
- **字数统计**：每章记录「目标字数」和「已写字数」，自动汇总全作品总字数
- **进度可视化**：
  - 章节行内显示状态图标与进度条（已写字数/目标字数）
  - 顶部固定显示作品总进度（如"已完成 12/30 章 · 总字数 45,000/120,000"）
  - 时间轴视图上的里程碑标记
- **加分项（暂缓，后续可选加）**：每日目标字数、连续写作打卡

### 模块 3：AI 润色（独立模块）

- **入口独立**：软件内单独的「AI 助手」页签，与大纲界面完全分开，互不读取数据
- **使用流程**：
  1. 用户把要润色的段落**手动粘贴**到聊天框
  2. 选择润色需求（更流畅 / 更文艺 / 更简洁 / 扩写 / 压缩 / 改风格等，预设几个常用按钮）
  3. 软件自动打开/唤起 DeepSeek 网页版，输入内容并发送
  4. 自动读取 AI 回复并显示在聊天框里，可一键复制
- **隐私隔离（硬性要求）**：
  - 大纲、作品文本、进度数据**永远只存在本地**，AI 模块完全无法读取
  - **只有用户主动粘贴进聊天框的文字**才会被发送给 AI
  - 发送前界面明确提示："以下内容将发送至 DeepSeek 服务器"
- **接入方式：自动操作 DeepSeek 网页版**（详见第三节的风险说明）

### 模块 4：数据与本地存储（硬性要求）

- 所有数据（大纲、进度、作品文本、备注）只保存在本机硬盘的本地文件里
- **不联网也能正常使用大纲和进度功能**（只有 AI 润色需要联网）
- 支持**导出为 Markdown / TXT**（大纲结构 + 备注，纯文本格式，便于备份和投稿）
- 软件升级不影响用户数据（数据结构变化时做自动迁移/兼容）

### 模块 5：界面设计

- 风格：简洁、清爽、留白多，避免花哨
- 配色：**白色为主 + 淡蓝色点缀**
- 布局设想：
  - 顶部：作品切换 + 总进度概览条
  - 左侧：大纲树（树状图）
  - 中间：选中章节的详情/备注/字数面板
  - 右上角入口：切换到「时间轴视图」「AI 助手」
- 全中文界面

### 模块 6：Git/GitHub 备份（2026-01 追加，用户确认只做 Git/GitHub 方案）

- 需求来源：用户希望"保存进度 → 修改 → 出错后回到上一次备份"
- 方案：**Git 版本管理 + GitHub 私有仓库云备份**（不做软件内置快照）
  - 数据仓库：`<userData>` 目录，只跟踪 `data/`（写作数据），忽略浏览器缓存
  - 应用内「⏺ 备份」面板：检测 Git、初始化仓库、立即备份（提交+推送）、撤销到上次备份、连接 GitHub 远程、查看备份历史
  - 附带批处理脚本：一键备份 / 恢复到上次备份 / 查看备份历史（不打开软件也能用）
  - 首次推送时弹出浏览器登录 GitHub 授权一次
- 待用户操作：下载安装 Git（https://git-scm.com/download/win），在 GitHub 创建私有仓库

## 三、自动操作 DeepSeek 网页版 —— 说明与风险（用户已确认接受）

实现思路：软件调用你电脑上已安装的浏览器（Chrome / Edge），用**你已登录的 DeepSeek 账号**自动完成"输入文字 → 发送 → 读取回复"。

需要你知道并接受的风险：

1. **依赖网页版登录**：需要你电脑的浏览器里已登录 DeepSeek 网页版；登录过期后需要重新登录
2. **可能失效**：DeepSeek 网页改版后，软件需要更新适配（这是软件维护成本，不是死路）
3. **账号风险**：自动化操作理论上可能违反 DeepSeek 网页版服务条款，存在账号被限制的极小概率；建议使用常用账号，不要在里面操作敏感内容
4. **频率限制**：网页版可能有每日提问次数/长度限制，受 DeepSeek 官方策略影响
5. **纯本地直连**：软件直接操作你本机浏览器，不经过任何第三方服务器中转

## 四、技术方案简述（用户了解即可，无需懂细节）

- **形态**：Windows 桌面软件，双击安装、打开即用
- **框架**：桌面应用壳（Electron 或 Tauri）+ 前端界面
- **数据**：本地文件存储（结构化 JSON + 作品正文文件），放在用户数据目录
- **浏览器自动化**：Playwright / puppeteer-core 驱动本机 Chrome 或 Edge（复用用户已登录的会话）
- **开发语言**：TypeScript / JavaScript 为主

## 五、确认结果

- 浏览器：用户不确定 → 软件启动时自动检测本机已安装的 Chrome / Edge，并显示选择界面，选中后记住（可改）
- DeepSeek 账号：已有 ✓
- 需求确认：**全部确认，开始开发** ✓

## 六、参考资料

- Scrivener（行业标杆写作软件）大纲与卡片墙设计：https://www.literatureandlatte.com/scrivener/overview
- DeepSeek 官方 API 文档（备用方案，若网页版方案不可行时改用）：https://api-docs.deepseek.com/zh-cn/quick_start/pricing/
